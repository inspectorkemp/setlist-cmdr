# Core framework
fastapi==0.135.1

# ASGI server
uvicorn==0.42.0

# WebSocket support (required for live sync)
websockets==16.0

# Form/file upload parsing — needed for CSV import
python-multipart==0.0.22
